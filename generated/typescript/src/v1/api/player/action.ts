// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.5
//   protoc               v5.28.3
// source: v1/api/player/action.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "v1.api.player";

export interface PlayerAction {
  /** / Unique identifier of the player */
  playerId: string;
  /** / Unique identifier of the session */
  sessionId: string;
  /** / Unique identifier of the game */
  gameId: string;
  /** / Type of action */
  type: PlayerAction_ActionType;
  /** / Position of the player (optional) */
  position?:
    | PlayerAction_Position
    | undefined;
  /** / Velocity of the player (optional) */
  velocity?:
    | PlayerAction_Velocity
    | undefined;
  /** / Custom actions the player can take */
  action: { [key: string]: PlayerAction_PlayerAction };
  timestamp: number;
}

/** / Type of action */
export enum PlayerAction_ActionType {
  /** CUSTOM - / Custom action */
  CUSTOM = 0,
  /** MOVE - / Move action */
  MOVE = 1,
  /** JUMP - / Jump action */
  JUMP = 2,
  /** SHOOT - / Shoot action */
  SHOOT = 3,
  /** PICKUP - / Pickup action */
  PICKUP = 4,
  /** DROP - / Drop action */
  DROP = 5,
  /** USE - / Use action */
  USE = 6,
  /** INTERACT - / Interact action */
  INTERACT = 7,
  UNRECOGNIZED = -1,
}

export function playerAction_ActionTypeFromJSON(object: any): PlayerAction_ActionType {
  switch (object) {
    case 0:
    case "CUSTOM":
      return PlayerAction_ActionType.CUSTOM;
    case 1:
    case "MOVE":
      return PlayerAction_ActionType.MOVE;
    case 2:
    case "JUMP":
      return PlayerAction_ActionType.JUMP;
    case 3:
    case "SHOOT":
      return PlayerAction_ActionType.SHOOT;
    case 4:
    case "PICKUP":
      return PlayerAction_ActionType.PICKUP;
    case 5:
    case "DROP":
      return PlayerAction_ActionType.DROP;
    case 6:
    case "USE":
      return PlayerAction_ActionType.USE;
    case 7:
    case "INTERACT":
      return PlayerAction_ActionType.INTERACT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PlayerAction_ActionType.UNRECOGNIZED;
  }
}

export function playerAction_ActionTypeToJSON(object: PlayerAction_ActionType): string {
  switch (object) {
    case PlayerAction_ActionType.CUSTOM:
      return "CUSTOM";
    case PlayerAction_ActionType.MOVE:
      return "MOVE";
    case PlayerAction_ActionType.JUMP:
      return "JUMP";
    case PlayerAction_ActionType.SHOOT:
      return "SHOOT";
    case PlayerAction_ActionType.PICKUP:
      return "PICKUP";
    case PlayerAction_ActionType.DROP:
      return "DROP";
    case PlayerAction_ActionType.USE:
      return "USE";
    case PlayerAction_ActionType.INTERACT:
      return "INTERACT";
    case PlayerAction_ActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface PlayerAction_ActionEntry {
  key: string;
  value: PlayerAction_PlayerAction | undefined;
}

/** / Position of the player */
export interface PlayerAction_Position {
  x: number;
  y: number;
  z: number;
}

/** / Velocity of the player */
export interface PlayerAction_Velocity {
  x: number;
  y: number;
  z: number;
}

/** / Custom actions the player can take */
export interface PlayerAction_PlayerAction {
  stringValue?: string | undefined;
  intValue?: number | undefined;
  floatValue?: number | undefined;
  boolValue?: boolean | undefined;
}

function createBasePlayerAction(): PlayerAction {
  return {
    playerId: "",
    sessionId: "",
    gameId: "",
    type: 0,
    position: undefined,
    velocity: undefined,
    action: {},
    timestamp: 0,
  };
}

export const PlayerAction: MessageFns<PlayerAction> = {
  encode(message: PlayerAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.position !== undefined) {
      PlayerAction_Position.encode(message.position, writer.uint32(42).fork()).join();
    }
    if (message.velocity !== undefined) {
      PlayerAction_Velocity.encode(message.velocity, writer.uint32(50).fork()).join();
    }
    Object.entries(message.action).forEach(([key, value]) => {
      PlayerAction_ActionEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    if (message.timestamp !== 0) {
      writer.uint32(64).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.position = PlayerAction_Position.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.velocity = PlayerAction_Velocity.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = PlayerAction_ActionEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.action[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerAction {
    return {
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      type: isSet(object.type) ? playerAction_ActionTypeFromJSON(object.type) : 0,
      position: isSet(object.position) ? PlayerAction_Position.fromJSON(object.position) : undefined,
      velocity: isSet(object.velocity) ? PlayerAction_Velocity.fromJSON(object.velocity) : undefined,
      action: isObject(object.action)
        ? Object.entries(object.action).reduce<{ [key: string]: PlayerAction_PlayerAction }>((acc, [key, value]) => {
          acc[key] = PlayerAction_PlayerAction.fromJSON(value);
          return acc;
        }, {})
        : {},
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PlayerAction): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.type !== 0) {
      obj.type = playerAction_ActionTypeToJSON(message.type);
    }
    if (message.position !== undefined) {
      obj.position = PlayerAction_Position.toJSON(message.position);
    }
    if (message.velocity !== undefined) {
      obj.velocity = PlayerAction_Velocity.toJSON(message.velocity);
    }
    if (message.action) {
      const entries = Object.entries(message.action);
      if (entries.length > 0) {
        obj.action = {};
        entries.forEach(([k, v]) => {
          obj.action[k] = PlayerAction_PlayerAction.toJSON(v);
        });
      }
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerAction>, I>>(base?: I): PlayerAction {
    return PlayerAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerAction>, I>>(object: I): PlayerAction {
    const message = createBasePlayerAction();
    message.playerId = object.playerId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.gameId = object.gameId ?? "";
    message.type = object.type ?? 0;
    message.position = (object.position !== undefined && object.position !== null)
      ? PlayerAction_Position.fromPartial(object.position)
      : undefined;
    message.velocity = (object.velocity !== undefined && object.velocity !== null)
      ? PlayerAction_Velocity.fromPartial(object.velocity)
      : undefined;
    message.action = Object.entries(object.action ?? {}).reduce<{ [key: string]: PlayerAction_PlayerAction }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = PlayerAction_PlayerAction.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePlayerAction_ActionEntry(): PlayerAction_ActionEntry {
  return { key: "", value: undefined };
}

export const PlayerAction_ActionEntry: MessageFns<PlayerAction_ActionEntry> = {
  encode(message: PlayerAction_ActionEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      PlayerAction_PlayerAction.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerAction_ActionEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerAction_ActionEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = PlayerAction_PlayerAction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerAction_ActionEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? PlayerAction_PlayerAction.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: PlayerAction_ActionEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = PlayerAction_PlayerAction.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerAction_ActionEntry>, I>>(base?: I): PlayerAction_ActionEntry {
    return PlayerAction_ActionEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerAction_ActionEntry>, I>>(object: I): PlayerAction_ActionEntry {
    const message = createBasePlayerAction_ActionEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? PlayerAction_PlayerAction.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBasePlayerAction_Position(): PlayerAction_Position {
  return { x: 0, y: 0, z: 0 };
}

export const PlayerAction_Position: MessageFns<PlayerAction_Position> = {
  encode(message: PlayerAction_Position, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(13).float(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(21).float(message.y);
    }
    if (message.z !== 0) {
      writer.uint32(29).float(message.z);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerAction_Position {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerAction_Position();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.x = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.y = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.z = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerAction_Position {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
    };
  },

  toJSON(message: PlayerAction_Position): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerAction_Position>, I>>(base?: I): PlayerAction_Position {
    return PlayerAction_Position.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerAction_Position>, I>>(object: I): PlayerAction_Position {
    const message = createBasePlayerAction_Position();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.z = object.z ?? 0;
    return message;
  },
};

function createBasePlayerAction_Velocity(): PlayerAction_Velocity {
  return { x: 0, y: 0, z: 0 };
}

export const PlayerAction_Velocity: MessageFns<PlayerAction_Velocity> = {
  encode(message: PlayerAction_Velocity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(13).float(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(21).float(message.y);
    }
    if (message.z !== 0) {
      writer.uint32(29).float(message.z);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerAction_Velocity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerAction_Velocity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.x = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.y = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.z = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerAction_Velocity {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
    };
  },

  toJSON(message: PlayerAction_Velocity): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerAction_Velocity>, I>>(base?: I): PlayerAction_Velocity {
    return PlayerAction_Velocity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerAction_Velocity>, I>>(object: I): PlayerAction_Velocity {
    const message = createBasePlayerAction_Velocity();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.z = object.z ?? 0;
    return message;
  },
};

function createBasePlayerAction_PlayerAction(): PlayerAction_PlayerAction {
  return { stringValue: undefined, intValue: undefined, floatValue: undefined, boolValue: undefined };
}

export const PlayerAction_PlayerAction: MessageFns<PlayerAction_PlayerAction> = {
  encode(message: PlayerAction_PlayerAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringValue !== undefined) {
      writer.uint32(18).string(message.stringValue);
    }
    if (message.intValue !== undefined) {
      writer.uint32(24).int32(message.intValue);
    }
    if (message.floatValue !== undefined) {
      writer.uint32(37).float(message.floatValue);
    }
    if (message.boolValue !== undefined) {
      writer.uint32(40).bool(message.boolValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerAction_PlayerAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerAction_PlayerAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stringValue = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.intValue = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.floatValue = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.boolValue = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerAction_PlayerAction {
    return {
      stringValue: isSet(object.stringValue) ? globalThis.String(object.stringValue) : undefined,
      intValue: isSet(object.intValue) ? globalThis.Number(object.intValue) : undefined,
      floatValue: isSet(object.floatValue) ? globalThis.Number(object.floatValue) : undefined,
      boolValue: isSet(object.boolValue) ? globalThis.Boolean(object.boolValue) : undefined,
    };
  },

  toJSON(message: PlayerAction_PlayerAction): unknown {
    const obj: any = {};
    if (message.stringValue !== undefined) {
      obj.stringValue = message.stringValue;
    }
    if (message.intValue !== undefined) {
      obj.intValue = Math.round(message.intValue);
    }
    if (message.floatValue !== undefined) {
      obj.floatValue = message.floatValue;
    }
    if (message.boolValue !== undefined) {
      obj.boolValue = message.boolValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerAction_PlayerAction>, I>>(base?: I): PlayerAction_PlayerAction {
    return PlayerAction_PlayerAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerAction_PlayerAction>, I>>(object: I): PlayerAction_PlayerAction {
    const message = createBasePlayerAction_PlayerAction();
    message.stringValue = object.stringValue ?? undefined;
    message.intValue = object.intValue ?? undefined;
    message.floatValue = object.floatValue ?? undefined;
    message.boolValue = object.boolValue ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
