syntax = "proto3";

package game;

enum InstanceState {
  INITIALIZING = 0;
  RUNNING = 1;
  PAUSED = 2;
  CRASHED = 3;
  ABORTED = 4;
  SHUTTING_DOWN = 5;
  STOPPED = 6;
}

message Instance {
  InstanceState state = 1;
  optional string game_id = 2;
  optional string data = 3;
}